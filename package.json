{
  "name": "band-together",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Low-latency P2P band practice app — browser-based, no server required",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@trystero-p2p/nostr": "^0.23.0"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
